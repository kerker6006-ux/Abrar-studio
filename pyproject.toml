[project]
name = "abrar-studio"
version = "3.0.0"
description = "Local-first 2D Korean motion-drama production studio"
requires-python = ">=3.11"
dependencies = [
  "Pillow>=10.4,<12",
  "packaging>=24,<26"
]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
